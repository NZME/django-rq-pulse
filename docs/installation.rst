============
Installation
============

At the command line::

    $ easy_install django-rq-pulse

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-rq-pulse
    $ pip install django-rq-pulse
